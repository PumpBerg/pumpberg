[build]
builder = "NIXPACKS"
buildCommand = "npm install"

[deploy]
startCommand = "npx tsx server.mjs"
healthcheckPath = "/health"
healthcheckTimeout = 15
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
